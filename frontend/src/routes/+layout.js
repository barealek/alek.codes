
// Prerender the page so it can be statically exported and served via CDN
export const prerender = true

// Disable server-side rendering globally
export const ssr = false

// Enable trailing slashes for all paths, so pages are directly accessible
export const trailingSlash = 'always'